const form=document.querySelector('#loginForm'),error=document.querySelector('#loginError'),submit=document.querySelector('#loginSubmit'),password=document.querySelector('#password');
const recoveryLink=document.createElement('a');recoveryLink.href='/forgot-password';recoveryLink.textContent='Забыли пароль?';recoveryLink.style.cssText='color:var(--cyan);text-decoration:none';document.querySelector('.login-options>span:last-child').replaceWith(recoveryLink);
document.querySelector('#togglePassword').onclick=e=>{const visible=password.type==='text';password.type=visible?'password':'text';e.currentTarget.textContent=visible?'◉':'◌';e.currentTarget.setAttribute('aria-label',visible?'Показать пароль':'Скрыть пароль')};
form.onsubmit=async e=>{e.preventDefault();error.textContent='';submit.disabled=true;submit.classList.add('loading');const values=Object.fromEntries(new FormData(form));try{const response=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(values)});if(!response.ok){const body=await response.json();throw new Error(body.detail||'Не удалось войти')}location.replace('/')}catch(err){error.textContent=err.message;submit.disabled=false;submit.classList.remove('loading');password.select()}};

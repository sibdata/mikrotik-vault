(() => {
  const credentialStyles = document.createElement('style'); credentialStyles.textContent = '.saved-credentials{display:block;margin:-8px 0 14px;color:var(--acid);font-size:8px;line-height:1.5}.saved-credentials[hidden]{display:none}'; document.head.appendChild(credentialStyles);
  const nav = document.querySelector('nav');
  const storageLink = document.createElement('a'); storageLink.href = '#'; storageLink.id = 'storageBtn'; storageLink.innerHTML = '<span>☁</span> S3-хранилище <i id="storageNavState">—</i>';
  nav.appendChild(storageLink);
  const headerActions = document.querySelector('.header-actions');
  const headerButton = document.createElement('button'); headerButton.className = 'ghost storage-header-button'; headerButton.innerHTML = '☁ <span>S3-хранилище</span>';
  headerActions.insertBefore(headerButton, document.querySelector('#addBtn'));
  const dialog = document.createElement('dialog'); dialog.id = 'storageDialog'; dialog.innerHTML = `<form id="storageForm"><div class="dialog-head"><div><span class="eyebrow">OBJECT STORAGE</span><h2>Подключить S3-хранилище</h2><p>RouterVault проверит доступ к bucket перед сохранением настроек.</p></div><button type="button" class="icon" id="closeStorage">×</button></div><div class="storage-state" id="storageState"><span>☁</span><p><b>Хранилище не настроено</b><small>Добавьте реквизиты S3-совместимого сервера</small></p></div><label>Endpoint<input name="endpoint" type="url" placeholder="https://s3.example.com"></label><div class="split equal"><label>Регион<input name="region" required value="us-east-1" placeholder="us-east-1"></label><label>Bucket<input name="bucket" required placeholder="mikrotik-backups"></label></div><div class="split equal"><label>Access Key<input name="access_key" autocomplete="off" placeholder="Введите access key"></label><label>Secret Key<input name="secret_key" type="password" autocomplete="new-password" placeholder="Введите secret key"></label></div><small class="saved-credentials" id="savedCredentials" hidden>✓ Ключи сохранены. Оставьте поля пустыми, чтобы использовать их для проверки и сохранения.</small><div class="split equal"><label>Адресация<select name="addressing_style"><option value="path">Path style</option><option value="virtual">Virtual hosted</option></select></label><label>Шифрование на сервере<select name="sse"><option value="AES256">AES-256</option><option value="aws:kms">AWS KMS</option><option value="none">Не запрашивать</option></select></label></div><div class="storage-actions"><button type="button" class="connection-check" id="checkStorage"><span>⌁</span><b>Проверить подключение</b><i>С сохранёнными ключами</i></button><button class="primary storage-save" id="saveStorage">Сохранить хранилище <span>→</span></button></div><p class="error" id="storageError"></p><div class="secure-note"><span>⌾</span><p><b>Ключи хранятся зашифрованными</b><small>Access Key и Secret Key никогда не возвращаются из API обратно в браузер.</small></p></div></form>`; document.body.appendChild(dialog);
  const form = document.querySelector('#storageForm'), state = document.querySelector('#storageState'), error = document.querySelector('#storageError'), checkButton = document.querySelector('#checkStorage'), saveButton = document.querySelector('#saveStorage');
  const bucketHint = document.createElement('small'); bucketHint.className = 'bucket-hint'; bucketHint.textContent = 'Введите своё название. Если bucket отсутствует, он будет создан при сохранении.'; bucketHint.style.cssText = 'color:var(--muted);font-size:8px;line-height:1.4;font-weight:400'; form.bucket.closest('label').appendChild(bucketHint);
  const payload = () => Object.fromEntries(new FormData(form));
  async function refreshStorage() { try { const result = await api('/api/storage'); const marker = document.querySelector('#storageNavState'); marker.textContent = result.configured ? 'OK' : '!'; marker.className = result.configured ? 'connected' : ''; document.querySelector('#savedCredentials').hidden = !result.credentials_saved; if (result.configured) { form.endpoint.value = result.endpoint || ''; form.region.value = result.region; form.bucket.value = result.bucket; form.access_key.value = ''; form.secret_key.value = ''; form.access_key.placeholder = 'Сохранён — введите только для замены'; form.secret_key.placeholder = 'Сохранён — введите только для замены'; form.addressing_style.value = result.addressing_style; form.sse.value = result.sse; state.classList.add('connected'); state.innerHTML = `<span>✓</span><p><b>${escapeHtml(result.bucket)}</b><small>${escapeHtml(result.endpoint || 'Amazon S3')} · ${escapeHtml(result.region)}</small></p>`; } } catch {} }
  async function openStorage(event) { event?.preventDefault(); error.textContent = ''; dialog.showModal(); await refreshStorage(); }
  storageLink.onclick = openStorage; headerButton.onclick = openStorage; document.querySelector('#closeStorage').onclick = () => dialog.close();
  checkButton.onclick = async () => { error.textContent = ''; checkButton.disabled = true; checkButton.querySelector('b').textContent = 'Проверяем bucket…'; try { const result = await api('/api/storage/check', {method:'POST', body:JSON.stringify(payload())}); checkButton.classList.add('passed'); checkButton.querySelector('b').textContent = 'Подключение установлено'; checkButton.querySelector('i').textContent = result.message; toast(result.message); } catch (err) { checkButton.classList.remove('passed'); checkButton.querySelector('b').textContent = 'Проверка не пройдена'; checkButton.querySelector('i').textContent = 'Проверьте реквизиты'; error.textContent = err.message; } finally { checkButton.disabled = false; } };
  form.onsubmit = async event => { event.preventDefault(); error.textContent = ''; saveButton.disabled = true; saveButton.firstChild.textContent = 'Проверяем и сохраняем… '; try { const result = await api('/api/storage', {method:'POST', body:JSON.stringify(payload())}); toast(result.message); dialog.close(); await refreshStorage(); await load(); } catch (err) { error.textContent = err.message; } finally { saveButton.disabled = false; saveButton.firstChild.textContent = 'Сохранить хранилище '; } };
  refreshStorage();
})();
